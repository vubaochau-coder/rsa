from prime_generate import prime_generate


def rsa_process(message, bit):
    p= prime_generate(int(bit))
    q= prime_generate(int(bit))

def compareProcrss():
    msg = input('Enter message: ')
    
    bit = input('Enter bit to generate primes: ')
    
    